<Window x:Class="library.View.WindowPublish"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:library.View"
        mc:Ignorable="d"
        Title="Издательства" Height="450" Width="800">
    <StackPanel>
        <ListView x:Name="lvPublish">
            <ListView.View>
                <GridView>
                    <GridView.Columns>
                        <GridViewColumn Header="Код" Width="50"
                        DisplayMemberBinding="{Binding Id}"/>
                        <GridViewColumn Header="Название"
                        DisplayMemberBinding="{Binding NamePublish}"/>
                        <GridViewColumn Header="Адрес"
                        DisplayMemberBinding="{Binding Address}"/>
                        <GridViewColumn Header="Сайт"
                        DisplayMemberBinding="{Binding Site}"/>
                    </GridView.Columns>
                </GridView>
            </ListView.View>
        </ListView>
    </StackPanel>
</Window>

